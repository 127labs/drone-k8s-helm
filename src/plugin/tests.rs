// use super::*;
// use super::super::*;

#[test]
fn verify_upgrade_command() {
    // TODO: Figure out how to mock which function from plugin

    // config::tests::mock_plugin_env_vars();
    // config::tests::mock_plugin_values_env_vars();

    // let mut config = Config::default();
    // config.load();
    // config.parse_values();

    // let command = build_upgrade_command(&config);
}
