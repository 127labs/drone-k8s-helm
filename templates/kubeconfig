apiVersion: v1
clusters:
- cluster:
{% if tls == "true" %}
    insecure-skip-tls-verify: true
{% endif %}
    server: {{ master }}
  name: helm
contexts:
- context:
    cluster: helm
{% if namespace %}
    namespace: {{ namespace }}
{% endif %}
    user: helm
  name: helm
current-context: "helm"
kind: Config
preferences: {}
users:
- name: helm
  user:
    token: {{ token }}
